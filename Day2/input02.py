#age=input("Enter your age: ")
#print("Age*2 is",age*2)
#print(age*2)
#This is wrong because age is a string and we cannot multiply a string with an integer. We need to convert the string to an integer first. it will display the number twice.

age=int(input("Enter age: "))
print("Age*2 is:",age*2)